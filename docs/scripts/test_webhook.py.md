# `scripts/test_webhook.py`

## `_resolve_sha`

```python
def _resolve_sha(ref: str) -> str
```

Resolves a git reference (such as a branch name, tag, or partial hash) to its full SHA-1 hash.

## `main`

```python
def main()
```

Execute the primary entry point for the application.
